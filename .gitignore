# --- Environment ---
.env
.env.*
!.env.example

# --- Node / TypeScript ---
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Next.js ---
web/.next/
web/out/
web/next-env.d.ts

# --- Prisma ---
backend/prisma/*.db
backend/generated/

# --- React Native / Expo ---
mobile/.expo/
mobile/dist/
mobile/web-build/
# Native projects are generated by `expo prebuild`; keep them out of git.
mobile/android/
mobile/ios/
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision

# --- Editors / OS ---
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# --- Documentation (temporarily untracked, by owner request 2026-08-22) ---
# These stay on disk and are still the authoritative specs that AGENT.md's
# reading order points at — they are simply not committed for the moment.
# A fresh clone will NOT contain them; get them from the owner directly.
# Remove this block and re-add the folders when they should be tracked again.
docs/
docs-other/
